spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agent?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

  elasticsearch:
    uris: http://localhost:9200

deepseek:
  base-url: https://api.deepseek.com/v1
  api-key: ${DEEPSEEK_API_KEY}
  model-name: deepseek-chat
  temperature: 0.1
  max-tokens: 4096

embedding:
  base-url: http://localhost:11434
  model-name: modelscope.cn/Embedding-GGUF/bge-large-zh-v1.5:latest
  dimension: 1024
  batch-size: 32

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  prometheus:
    metrics:
      export:
        enabled: true

logging:
  level:
    com.agent: DEBUG
